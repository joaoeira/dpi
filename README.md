# Deep Policy Iteration (DPI)

Reference Python implementation for the Deep Policy Iteration method.

## Quick start

Create a virtual environment and install dependencies with uv:

```
python3 -m venv .venv
uv pip install -p .venv torch numpy scipy matplotlib tqdm
```

Run the smoke training script:

```
.venv/bin/python scripts/smoke_train.py
```
